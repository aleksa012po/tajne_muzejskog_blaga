import 'package:flutter/material.dart';

class ColoringImageSelectionScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Coloring Image Selection'),
      ),
      body: Center(
        child: Text('Coloring Image Selection Screen'),
      ),
    );
  }
}
